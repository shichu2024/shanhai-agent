# 修订回应与方案 V0.2 · 方案设计师（TASK-66 / WP-4A）

> **文档状态：** 对反方审查报告（TASK-66 线程，2026-09-25）的逐项回应与落稿表
> **修订载体：** 《01-第四阶段设计文档》整体升版 V0.2（同文件；V0.1 存于 `262ab1f`）
> **处置统计：** P0-1 接受（方向 1）+ P1×3 接受 + P2×2 接受 + P3×5 接受 + 特别要求#5 复杂度修剪三处（部分接受）；**无不接受项**
> **源码锚点独立复核声明：** 修订前本人对报告引用的五处锚点逐一复核（`executor.ts:139` / `executor.ts:181-183` / `toolExecutor.ts:123-132` / `toolExecutor.ts:163-181` / `db.ts` 增列清单），全部属实——V0.1 的四处「原样适用」断言系本人在未逐行核对执行内核的情况下外推，向审查方致认。

---

## 1. 逐项修订表

| 审查问题 | 处理结果 | 处理说明 |
|---|---|---|
| **P0-1** 子任务审批挂起与阻塞式嵌套结构性冲突 | **接受（方向 1：委托可重入设计）** | §4.4 整节重构：① 子 ApprovalRequiredSignal 在 impl 边界特判**上浮为父任务级挂起**（不进 attempt 异常归类——特判点即 `toolExecutor.ts:123-132` 的吞异常处）；② 父 snapshot 载荷增 childTaskId + pendingIndex +「委托进行中」标记；③ impl **幂等重入**（childTaskId 锚定：非终态续接、终态直返摘要）；④ resume 时序成文（先子后父 + 乱序容错：子 pending → 父幂等保持 Paused + 提示）；⑤ 崩溃三窗口矩阵显式化（窗口① 零新机制、窗口② 子终局父 Paused、窗口③ 双挂起）。**「零新机制」表述全部撤回**（§3 原则 5、§13-3、§5 StateManager 行同步改写）；新机制显式计入 D-30。方向 2 被否：砍掉「委托更高权限 Agent」一等用例与 §4.4 风险作用域规则自相矛盾；方向 3 被否：异步模型须新建等待/取消两套机制，成本账劣于方向 1 的三项集中特殊化。A-19/A-20 按新机制重写为可断言验收（两段审批端到端 / 幂等重入 / 三窗口各一用例） |
| **P1-1** task-delegate 在既有超时/重试语义下不可运行 | **接受** | §4.4 委托原语类别属性表：**超时豁免**（不受 `toolTimeoutMs`（executor.ts:139 硬编码 30000）约束，子任务自有预算与任务级超时）+ **attempt=1 禁重试**（impl 含创建子任务副作用，重试 = 重复子任务）；「归因同构成立、超时重试同构不成立」的修正表述落稿；A-20-③ 断言（构造子任务失败 → 无第二次 impl 调用、无重复子任务） |
| **P1-2** targetWhitelist「零变更」表述不成立 | **接受** | §4.4 闸门顺序修订（A2 §8 v1.2 增补）：`声明检查 → 目标白名单（等级无关）→ 等级分支 → L2 参数范围`；task-delegate 参数映射 `agentId`，既有 L2 工具维持字面 `target` 检查（**行为零变更 + 回归断言随批**）；paramRanges 维持 L2-only。「Spec 层零新字段成立、执行层需 gate 扩展」的诚实表述落稿；A-20-① 断言（agentId 越白名单 → `target_not_whitelisted`） |
| **P1-3** MCP 受控字段候选无强制消费方 | **接受（选方案①）** | §4.2 步骤 2 修订：**候选定位 = 编写建议，非执行面；执行以 Spec 声明为唯一真源**（底线 1 单一真源精神）；connect 自动生成 Spec 声明片段（tools 数组 JSON）供 agent 作者直接复制——搬运责任显式归 agent 作者，消除无人负责环节。不选方案②的理由：gate 增读 registry 侧 controlledFieldsSchema 会制造 registry 与 Spec **两处执行真源**，同一工具两面声明漂移时闸门行为不可预测，违反「Spec 必须被 Runtime 强制执行」底线的单一真源精神；可见性缺口由 review diff 清单（登记元数据展示）覆盖。**承担的已知风险：** agent 作者不镜像则候选零强制力——缓解 = connect 片段零成本搬运 + A-13 前提显式化（「受控字段执行面 = Spec 声明」随断言成文，验收者可见） |
| **P2-1** parentTaskId 锚点事实错误 | **接受** | §1 与 §4.4/A6 增补段修正：列**未落地**（A3 §4 规格行存在、`db.ts` 迁移缺失），本批**补迁移** + A3 注记登记该规格-实现存量漂移事实；§5 TaskManager 行改 ADD COLUMN ×2。本人复核确认 src 全目录零命中——V0.1 引用规格行而未核对实现，锚点审计失真致认 |
| **P2-2** 父取消感知粒度未定义 | **接受** | §4.4 取消传播行重写：**父取消检查函数作为参数注入嵌套子循环**，子循环在原子调用边界代查父 abortRequested/cancel → 感知即中止（子 superseded + 父终局）；显式承认这是**新管道**计入 D-31；「abortRequested 既有机制」对 impl 内部场景不成立的表述撤回；A-20-⑤ 断言 |
| **P3-1** 无 TTY 不可驱动 | **接受** | §4.2 步骤 1：`--yes` 非交互档（全部按缺省 L3）；留痕落点 = 审计载荷（registeredBy + 评级），非交互过程 |
| **P3-2** 依赖评估探针增列 | **接受** | §11 D-28 + §12 假设 #2：版本锁定 + lockfile 纪律 + 传递依赖许可面 |
| **P3-3** registeredBy 自称字段 | **接受** | §9-6 成文：单用户 CLI 信任模型下的辅助留痕，非审计强证据（强证据 = 审计事件时间序与载荷） |
| **P3-4** T3 envRefs 扫描面澄清 | **接受** | §9-4：规则实际保护 config.example.json 与误提交副本/样例；验收口径以此为准 |
| **P3-5** 批次三串并行并存 | **接受** | §8 批次三前置依赖改为「批次二（定串行）」；§15 同步 |
| **特别要求#5** 不必要复杂度 | **部分接受（三处修剪，一处保留）** | ① health 独立命令**删除**（并入 connect 输出往返计时）；② 真实 server 演示**降为择项增强**（夹具覆盖语义面，非门槛）；③ A-22 contentHash 一致性断言**删**（只读现算无漂移面；contentHash 字段保留，密钥零命中断言保留）；④ **保留**：注入防护边界标记不删——审查报告自身在风险维度确认其为必要最小对策（D-13 同款），非复杂度 |

## 2. 复审请求

按闭环合同与审查报告 §七结论：**请反方审查官仅就 P0-1 修复（D-30 委托可重入设计）复审**——重点：① 信号上浮特判点是否充分（其他嵌套信号类型（abort/timeout）穿透 impl 边界的归类路径，已列入假设 #3 扩展面与 §14.4-① 复审焦点）；② 崩溃窗口矩阵 ②③ 的处置是否自洽；③ A-19/A-20 新断言是否覆盖报告 §七要求的三条（两段审批端到端 / 幂等重入 / 超时豁免禁重试——均已入表）。其余 P1/P2/P3 修订若复审时发现落稿偏差一并指出；已裁事项（D-27 缺省 L3、深度上限 1）不重开。复审通过后呈综合决策官终审冻结 D-27~D-34。

---

*本表与 01 号 V0.2 同 commit 落稿。01 号文档内所有修订处以「V0.2 修订 / P0-1 / P1-x / P2-x / P3-x」标记可检索。*
