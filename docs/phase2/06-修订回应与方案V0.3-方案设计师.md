# 修订回应与方案 V0.3 · 方案设计师（TASK-43 / WP-2A）

> **文档状态：** 对反方复审意见（04 号）按决策官预裁决（05 号）的逐项落稿表
> **作者角色：** 方案设计师
> **日期：** 2026-09-19
> **修订载体：** 《01-第二阶段设计文档》整体升版 V0.3（同文件；V0.2 存于 `e5a962c`）
> **处置统计：** R-1～R-5 全部按预裁决表落稿；无不接受、无偏离项。

---

## 1. 逐项落稿表（核对基准 = 05 号预裁决表）

| 编号 | 预裁决动作 | 落稿位置 | 落稿内容 |
|---|---|---|---|
| **R-1**（P1） | 采纳反方方案全文 | 01 号 §4.1-4（流程图 + 规则 2/3 + 结构事实段）、§4.1-6（approve/resume 命令行）、§5-F-1/F-2-⑥、§10 D-18、批次一 DoD-② | **approve 只写 `decision=approved` + Trace(approval_decided)，任务保持 Paused**；**`Paused → Running` 迁移权归 `--resume` 进程**（校验 → 迁移[先持久化] → 同进程反序列化 → 继续执行）；崩溃窗口消解论证落稿（任何时刻崩溃，任务要么 Paused[合法长驻] 要么已在执行进程手中——不存在「Running 且无执行进程」悬空态）；DoD-② 增断言「approve 崩溃于 spawn 前（或 --detach 后）任务停留 Paused，manual-resume 仍可达终态」；`resumedBy` 双值均真实可达；spawn 失败不回滚 decision、不影响任务状态（manual-resume 兜底） |
| **R-2**（P2） | 方案①基线列 | 01 号 §4.4-2（MemoryRecord 表）、§4.4-3（状态机图 + 可判性段）、§10 D-13、批次三 DoD-② | MemoryRecord 增 **`evidenceCountAtLastTransition`** 列；恢复边条件 = `evidenceCount − evidenceCountAtLastTransition ≥ 2`（独立 taskId）；**基线快照与状态迁移落库同事务**（预裁决落稿注意原文落稿，防 R-1 同型新窗口）；批次三 DoD 单测含基线同事务写入断言 |
| **R-3**（P2） | 方案①恢复拦截 | 01 号 §4.1-3（末段回退 + 三处同步声明）、§4.11 A2 行、批次一 DoD-⑥ | 运行期调用点（复验后、调用前）登记等级已变 L3 → **按当前登记等级拦截（`policy_denied(risk_level_blocked)` 可达）**，恢复第一阶段 A2 §4-2 防御纵深行为；裁决理由落稿（版本固定保护 Spec 语义，不外推到工具本体已重判定的执行放行——安全阀非语义锚；与「等级只能升不能降」同向）；三处同步：§4.1-3 + §4.11 A2 增补行 + **A2 §4-2 修订注记义务**（WP-2A 规格扩展定稿时执行）；批次一 DoD 增 L2→L3 调用点拦截回归单测 |
| **R-4**（P3） | 采纳 | 01 号 §4.1-4 规则 1/4、§4.11 A3 行、批次一 DoD-④ | 恢复扫描 / CrashRecovery 终局迁移**顺带删除该 taskId 的 PauseSnapshot**（覆盖「snapshot 写后迁移前被 kill」的孤儿快照——该任务从未进入 Paused，不触发常规清理）；批次一 DoD 增孤儿快照清理单测 |
| **R-5**（P3） | 采纳（随 R-3） | 01 号 §4.1-3 可见性段、§4.7 report、§4.11 A2 行 | report 旁挂**「受工具升级影响的 Spec」只读单列**（Spec 声明等级 < 当前登记等级的存量引用清单），与 staleQueued/stalePaused 同一可见性模式 |

## 2. 三挑战点与预确认项核对

| 项 | 预裁决 | 落稿 |
|---|---|---|
| approve/resume 责任分离 | 随 R-1 状态机层面结清 | 01 号 §9.4-① 改写为「已结清」 |
| L2→L3 静默堆积 | 失败路由维持 + R-5 单列 | §9.4-② 改写为「已结清」（路由维持 + 单列 + R-3 回退） |
| injection 默认 off | 维持 off，终审正式冻结 | §9.4-③ 改写为「决策官确认维持 off」 |
| D-9/D-12/D-13/D-17 预确认 | 终审时正式写入冻结决策表 | 01 号 §10 决策表维持「待裁决」状态待终审——预裁决不改标注，避免与终审流程混淆 |

## 3. V0.2 → V0.3 变更清单（供复核导航）

| 变更 | 位置 | 对应 |
|---|---|---|
| §4.1-4 流程图重写（迁移移入 resume）+ 「迁移权归 resume」结构事实段 + 规则 1/2/3/4 修订 | 01 号 | R-1 / R-4 |
| §4.1-6 approve 行（只写 decision）与 resume 行（持有迁移权） | 01 号 | R-1 |
| §4.1-3 调用点回退拦截 + 三处同步声明 + report 单列 | 01 号 | R-3 / R-5 |
| §4.4-2 基线列；§4.4-3 恢复边条件改基线差值 + 可判性段 | 01 号 | R-2 |
| §4.7 report 旁挂单列 | 01 号 | R-5 |
| §4.10 批次一 DoD-②④⑥ 增补；批次三 DoD-② 基线断言 | 01 号 | R-1/R-4/R-3/R-2 |
| §4.11 A2/A3 增补行修订 | 01 号 | R-3 / R-4 / R-5 |
| §5 F-1/F-2 流程更新（含 F-2-⑥ manual-resume 场景） | 01 号 | R-1 |
| §9.4 三挑战点全部改写为「已结清」 | 01 号 | 预裁决三立场 |
| §10 D-13/D-18 修订注记 | 01 号 | R-2 / R-1 |
| 04/05/06 号归档（复审意见 / 预裁决 / 本表） | docs/phase2/ | 流程 |

## 4. 复核请求

按 05 号流程指令第 2 条：请反方以预裁决表为核对基准复核 R-1～R-5 落稿情况（上表 §1 逐项可对），出具**复审通过声明**；已裁事项不重开。通过声明后即呈请综合决策官终审。

---

*V0.3 全文见 01 号文档（本 worktree HEAD）。全部为文本级修订，未动批次结构与已裁决决策点框架（05 号修订量评估确认）。*
