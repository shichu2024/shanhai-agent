# 第三阶段终验收报告（WP-3C / TASK-50）——反方审查官

> **验收对象：** WP-3B 四批次交付物（验收基线 main `d4433c4`，即 PR #12 merge commit）
> **验收人：** 反方审查官（独立复现模式：不采信任何交付自述，全部结论在本基线复跑/核对得出）
> **输入：** `docs/phase3/03-批次四-收官终检输入-A断言索引.md` + 设计文档 V0.2（D-19~D-26 冻结）+ A6 §8 v1.3
> **施工位置：** worktree `shanhai-3c`（分支 `wp-3c-acceptance-task50`，自 `d4433c4` 起）
> **日期：** 2026-09-20

## 1. 验收结论

**第三阶段（WP-3B 四批次 + 收官）终验收：通过。**

- A-1~A-11 全部断言在验收基线独立复现成立（逐条定向复跑，非全量一次性带过）；
- 四批次 DoD 静态全数核对成立；约束闭环（三底线 + T1/T2/T3 + 规格 V1 兼容零回填）核查无违例；
- 冻结决策抽查（D-11 / D-14 / D-21 / D-23 含批次四分子对称）无翻案；
- 本报告随附一项 P3 措辞修正（`src/config.ts` 注释，决策官 01a0bdcf 裁定「字段被忽略」口径）；
- 未发现 P0/P1/P2 缺陷；此前各批验收留档的全部遗留项（批次一 P2-1 / 批次三 P2 / 批次二 P3 / 批次一 P3）已于批次四全数收口。

## 2. 全量回归（验收基线 `d4433c4`，本终端实测）

| 项目 | 结果 | 对照硬约束 |
|---|---|---|
| `npx vitest run` | **206/206**（22 文件；含第一阶段 139 存量全程零回退链 139→164→175→190→206） | 零回退 ✓ |
| `npx vitest run --coverage` | 行覆盖 **90.94%** | ≥89.81% 硬底 ✓（较批次三 90.68% 上升） |
| `npx tsc --noEmit` | 零错 | ✓ |
| `npm run build` | 成功 | ✓ |
| `npm run release-scan` | **exit 0 零命中**；汇总行含新口径「二进制残余 0 个文件 / 不可读 0 个文件」 | T3 发布安全 ✓ |
| 真实 CLI exit 冒烟 | 仓库根=0 / 夹具根（豁免）=2 / `--allow-exempted-root`=1（四族阳性对照全命中）/ 根不存在=2；第二位置参数 → stderr 告警在场 | D-21 三元组 ✓ |
| `node docs/acceptance/p4-demo/walkthrough.mjs` | 全部通过 | ✓ |

## 3. A-1~A-11 逐条复现（按 03 索引定向复跑）

| # | 断言 | 复现方式 | 结果 |
|---|---|---|---|
| A-1 | 密钥→记忆→注入→provider system 无密钥（端到端） | `tests/dbRedaction.test.ts` 定向复跑（11/11，含 DoD-⑥ describe） | ✓ |
| A-2 | 终态任务 approve/deny 结构化拒绝 | `tests/approvalTerminal.test.ts`（9/9，五用例表 + 真实终态对照） | ✓ |
| A-3 | T3 CLI exit 三元组 | `tests/releaseScanCli.test.ts`（16/16）+ `tests/wp3b-batch4-releaseScan.test.ts`（5/5）+ 真实 CLI 五场景实测（§2） | ✓ |
| A-4 | 四落盘面行内零命中 + contextJson 结构完整性 + 实验路径继承 | `tests/dbRedaction.test.ts`（DoD-①/③/④ describe 在 11 例内） | ✓ |
| A-5 | 同一 redaction 管道实例（marker 注入一致性） | 同上（DoD-② 五面一致 + Trace/DB 字符串级全等） | ✓（断言形态既定不复议） |
| A-6 | inputHash 原文对账 + T1/T2 原样重跑 | 同上（跨任务同摘要用例）+ T1/T2 存量用例随全量 206 复跑 | ✓ |
| A-7 | rollback×canary 互斥 | `tests/batch3.test.ts`（15/15，§4.5-1） | ✓ |
| A-8 | report 分母/分子排除 cancelled + 零基线 insufficient-sample | `tests/batch3.test.ts`（§4.5-2）+ `tests/wp3b-batch4.test.ts`（11/11，A.2 分子对称含极端面） | ✓ |
| A-9 | 三清单消费同一常量源 | `tests/batch3.test.ts`（§4.4-1，含本地双份消亡反向断言） | ✓ |
| A-10 | dismiss 冷却窗 | `tests/batch3.test.ts`（§4.5-4：默认 7 天 / 可配 / 0=关闭 / 到期恢复） | ✓ |
| A-11 | 全程零回退 + 覆盖率 | 全量 206/206 + 90.94% + walkthrough（§2；与各批反方独立复跑记录互证） | ✓ |

## 4. 冻结决策抽查（终检建议 #3）

| 决策 | 抽查点 | 结果 |
|---|---|---|
| D-11 管道不可削 | `tests/redaction.test.ts`（7/7：空规则集仍过管道） | 无翻案 ✓ |
| D-14 幂等/聚合 | `tests/evolution.test.ts`（7/7：幂等键 agentId+trigger、聚合键 agentId、重聚合不重复） | 无翻案 ✓ |
| D-21 exit 三元组 | 真实 CLI 五场景 + 16+5 例（含不可读属 exit 2 的 P2-1 修订面） | 无翻案 ✓ |
| D-23 分母口径 | batch3 + wp3b-batch4 A.2（分子对称排除，批次三验收 P2 由批次四收口） | 无翻案 ✓ |
| 零数据回填 | 批三/批四新增列（`derivedVersionIds`/`dismissedAt`）均幂等 ADD COLUMN、默认值 `'[]'`/NULL 不回填（`src/db.ts` addColumn helper） | ✓ |

## 5. 约束闭环核查（终检建议 #4）

- **三底线**：Trace 先持久化后继续 / 密钥仅环境变量（`config.ts` fail-fast + T3 占位）/ 终态迁移单一归属——随 206 全量与 walkthrough 复跑 ✓；
- **T1/T2/T3 不回退**：T1/T2 存量断言随全量复跑；T3 真实 exit 0 + 阳性对照四族命中（allow 模式 exit 1）✓；
- **规格 V1 兼容迁移**：零 schema 破坏（仅幂等增列）、存量库行为兼容（scope 剥离为零迁移、config 加载只放宽）✓；
- **命名纪律登记**：消费点清单 §9（canaryRounds 切轮事件 / `*Hash` 后缀）成文在场，第三阶段无新增违例键 ✓。

## 6. 本报告随附修正（P3，决策官裁定口径）

`src/config.ts` 两处注释：「已写 scope 的条目被忽略」→ 明确为**「该字段被忽略（条目仍被接受）」**——实际行为（filter 只查 ruleId+pattern、map 剥离 scope 字段、条目被接受）自批次四起未变，本次仅修正注释措辞，零行为影响。修正后 `npx tsc --noEmit` 零错（vitest 全量 206/206 为修正前同基线跑出，注释变更不触及行为面）。

## 7. 缺陷登记

**无 P0/P1/P2。** 第三阶段验收期全部已知问题及其收口状态：

| 来源 | 问题 | 状态 |
|---|---|---|
| 批次一验收 P2-1 | 不可读 ≠ 二进制语义分裂 | 批次四收口（三态探测 + 单列计数 + exit 2）✓ |
| 批次一验收 P3-2/3/4 | 位置参数告警 / TDD 证据条款 / cwd 配置口径 | P3-2 批次四收口 ✓；P3-3 已转硬性流程条款（批二起合规）✓；P3-4 接受风险（既定） |
| 批次二验收 P3-1/2 | `*Hash` 键名信任 / redactReason 静默退化 | 前者命名纪律登记（清单 §9-2）✓；后者批次四补 stderr 痕迹 ✓ |
| 批次三验收 P2/P3 | report 分子分母不对称 / 切轮事件命名纪律 | 前者批次四收口 ✓；后者登记（清单 §9-1）✓ |
| 批次四验收 P3 | config.ts 注释措辞 | **本报告收口**（§6）✓ |

## 8. 验收声明

第三阶段实现（WP-3B 四批次：口径冻结+T3 收口 / DB 脱敏 / 演进同源+治理 / 留档收口）在验收基线 `d4433c4` 通过反方独立终验收。本报告与本 P 内的 P3 措辞修正是第三阶段最后变更；建议决策官按合入 owner 规则合入本归档 PR，第三阶段实现面就此冻结，转入用户验收。

——反方审查官，2026-09-20
