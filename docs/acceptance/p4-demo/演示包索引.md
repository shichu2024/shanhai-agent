# P-4 演示包索引（TASK-40，第一阶段演示轨）

> 依据：TASK-40 终审裁定（综合决策官，2026-09-19）——演示轨「不新跑，汇编既有真实证据」，两条真实路径记录已覆盖「README 快速开始原样走通」。本包不含任何新的真实模型运行。

## 组成

| 文件 | 作用 |
|---|---|
| `演示包索引.md`（本文） | 演示内容索引 + 讲解口径 |
| `walkthrough.mjs` | 走查脚本（只读，零模型调用）：机械复核归档证据并逐线输出讲解文本 |

运行方式（仓库根目录）：

```bash
git fetch origin worktree-wp-c-acceptance-task40   # 首次走查前拉取归档分支
node docs/acceptance/p4-demo/walkthrough.mjs       # 全部 PASS 即演示证据链完好
```

## 演示线（三条，按叙事顺序）

### 演示线 1 · C1 端到端真实业务只读任务（正面演示）

- **讲什么**：完整任务生命周期——Spec 注册/发布 → 任务创建（落库前校验 + 防御性复验）→ 执行 → Trace 落盘 → 终态与归因正确。
- **真实记录**：真实模型 glm-4.6；Agent `docs-analyst` 用 docs-list/docs-read 对本仓库 `docs/` 做只读分析并产出结构化摘要（25 文件 / 两子目录，内容核对无误）；5 次模型调用 / 26568 tokens / Trace 37 事件 / `succeeded`。
- **证据位置**：`worktree-wp-c-acceptance-task40` @ `e3a1c48`，`docs/acceptance/TASK-40-WP-C验收报告.md` §C1 + `docs/acceptance/task40/evidence/evidence-c1-f1.json`（键 `c1`）。

### 演示线 2 · F-1 缺陷存证（缺陷如何被验收暴露）

- **讲什么**：修复前的缺陷形态——CLI 分两进程 `task create` → `task run` 时，run 进程启动即执行崩溃恢复扫描，把 Queued 任务误判为 `Failed:Runtime(CrashRecovery)`，任务永不可经文档化工作流执行。
- **真实记录**：Trace 序列 `task_created → contract_checked → task_queued → crash_recovery_marked → task_failed`（无 `task_started`，`startedAt=null`——从未执行即被判死）。
- **证据位置**：同上 JSON（键 `f1_cli_defect`）。

### 演示线 3 · F-1 修复重验（闭环收口）

- **讲什么**：规格修订（A3 §6 收窄至仅 Running，`6761b3b`）→ 实现修复（`faabf68`）→ 重验通过。
- **真实记录**：真实模型 glm-4.6 补验（执行官环境缺密钥，由审查官环境按 README 快速开始原样执行）：进程 A register→release→create 后正常退出；进程 B 新进程启动扫描零迁移 → `task run` → `succeeded`（5 次调用 / 15456 tokens / Trace 46 事件 / `crash_recovery_marked` 0 次）。
- **证据位置**：`worktree-wp-c-acceptance-task40` @ `4b2c1a2`，`docs/acceptance/task40/TASK-40-F1闭环重验记录.md` + `docs/acceptance/task40/scripts/f1-twoprocess-real.mjs`。
- **修复最终态**：`wp-b-runtime-task39` @ `faabf68`（PR #1 集成分支，54/54 单测全绿）。

## 讲解口径备注

- 演示线 2/3 合起来是「规格冲突如何被验收暴露并闭环」的完整叙事，过程资产详见复盘文档 §4（`docs/phase1/第一阶段复盘-定稿.md`）。
- 运行数据（Trace JSONL、SQLite）在验收机本地 `data/acceptance-task40/`（gitignored）；本包只依赖 git 归档分支上的证据制品，可在任意持有该分支的环境复核。
