# 第五阶段设计修订回应与方案 V0.2 · 方案设计师

> 对象：反方审查报告（2026-09-26，TASK-75 线程）· 结论：有条件通过（P1×2 + P2×2 + P3×5）
> 修订载体：`docs/phase5/01-第五阶段设计文档-初稿-方案设计师.md` V0.1→V0.2（就地修订，PR #20 增量 commit）
> 审查报告对 §0 全部事实锚点的独立复核与 D-36 立场支持，我方确认收到并致谢——数据地基核查的先行义务经双向验证闭环。

## 修订总览

**9 项全部接受，无不接受项。** 其中 P1×2 + P2×2 随文修复（V0.2 正文已改），P3×5 亦全部随文修复（成本均为措辞/断言钉死级，无一项需要后置）。反方附加建议（derived statement 不含数字字符机械防线、D-36 注记措辞「schema 接受但零执行」）一并采纳。

## 修订表

| 审查问题 | 处理结果 | 处理说明 |
|---|---|---|
| P1-1 趋势分母「同源」机制错置（同常量 ≠ 同口径；report.ts:126 实际消费落盘谓词 countedInContractRate） | 已接受 | §4.3 任务面改为消费与 report **逐字相同 SQL 谓词**（`countedInContractRate = 1 AND status != 'cancelled'`）；常量（EXCLUDED_FROM_CONTRACT_RATE）维持写侧单一源地位（recorders 消费点不动）——写侧定事实、读侧共消费，常量变更后历史桶与新算桶共享同一落盘事实，两读侧永不分叉。A-29 改「同谓词」+ 新增回归断言「常量变更（测试内改 EXCLUDED_FROM_CONTRACT_RATE）后 trend 与 report 历史桶逐桶相等」（该断言在旧机制下必假、新机制下可过——即可失败的证明）。D-39 / §5 report 行 / §14.1-3 同步改写。 |
| P1-2 去重唯一索引跨全状态 vs「人工重加同义断言」自相矛盾 | 已接受（修复方案 a） | 唯一索引改**部分唯一索引** `WHERE status != 'retired'`（SQLite 支持）；derived 幂等口径同步改为「已存在同 (agentId, kind, statementDigest) 且非 retired 的行则跳过」；retired 行不阻挡人工重加（新行登记）、derived 不复活 retired 行（防误 dismiss 后自动回潮）。A-27 断言边界改写为「retired 不阻挡：人工重加同义断言成功入行、derived 重算不复活 retired 行」。§13-4 取舍重写——原辩护依据确被自身机制堵死，反方指正成立。选 a 而非 b/c：b（origin 维度去重）保留「manual 与 derived 同义断言各行」的割裂；c（冷却窗）引入时间维度机制而 capability 断言无 evolution 式 dismiss 频率治理需求；a 机制最小且语义最准（retired = 本条退场，非同义断言终身禁言）。 |
| P2-1 D-38 add 时证据强制使 14.4-2 草稿出路不可行 | 已接受（采反方倾向方案） | 证据强制力从 add 移至 **confirm**：add 允许 evidenceRefs 空（candidate 落库标注 `evidencePending: true`——「人类知道但暂无运行证据」草稿形态）；add 提供了 ref 但不可解析仍整单拒绝（**草稿可以无证据，不可以假证据**）；confirm 时强制 ≥1 且全部可解析。真正要保的不变量（无证据不进 active）保留，且与「derived 只生成不激活」对称。D-38 改「无证据不激活」；A-26 / §4.2 CLI / §6 流程 2（新增确认流程步）/ §9-4 同步改写；14.4-2 标记已结清。 |
| P2-2 A-30「声明面 ⊇ Card 字段子集」不可证伪 | 已接受 | §4.4 声明面钉死冻结清单：`INSIGHT_CARD_FIELD_KEYS = [agentId, versionId, specVersion, contentHash, mission, nonGoals, tools]`（常量导出 + 测试断言消费，D-41 纪律同款，Card 的 CARD_FIELD_KEYS 先例同构）；A-30 改为对该冻结清单的**逐字段存在性断言**——未定义子集的同义反复缺陷消除，断言可失败。 |
| P3-1 statement >2000 截断腐蚀去重键 | 已接受 | 截断告警改 **fail-fast 结构化拒绝**（第四阶段 resultMaxChars P3-2 先例）；§6 流程 2 边界、A-26 同步改写。拒绝优于截断的理由与反方一致：截断使两条同前缀长断言 digest 碰撞被误并。 |
| P3-2 R1「占该期失败总数」分母口径未钉死 | 已接受 | R1 分母钉死为与 trend/report 同谓词（countedInContractRate=1 AND status!='cancelled'）——§4.2 R1 判据行内成文；A-27 携带断言。第三口径不诞生。 |
| P3-3 statementDigest 规范化函数未定义 | 已接受 | 钉死为单一导出函数：sha256(NFC + trim + 内部连续空白折叠单空格 + 小写化)；manual/derived 两处共用同一函数（表结构注释成文）。 |
| P3-4 断言面桶取哪一列未定 | 已接受 | 钉死 **createdAt**（登记即事实）——§4.3 断言面行内成文。 |
| P3-5 coverage-from 单一标注低报任务/失败面覆盖 | 已接受 | 改**按面标注**：记忆面单独标注 `coverage-from-memory`（首个 memory_state_changed 时间戳）；任务面/失败面全史不标注。§4.3 诚实边界、A-29 同步改写。 |
| 附加① derived statement 不含数字字符机械防线（审查 §四-落地建议） | 已接受 | 入 A-27：`derived statement 不含数字字符`回归断言（一行测试），与模板冻结常量断言双锚。§12 假设 #2 同步注记。 |
| 附加② D-36 漂移注记措辞（审查 §三末） | 已接受 | 措辞钉死「**evaluationPolicy schema 接受但零执行**」，§0 与 D-36 成文，防 §12-8 担心的「本期实现了评估」误解。 |

## 对「如何证明修订更好」的回答（审查报告 §六-7）

两处 P1 修复各带一条**可失败的回归断言**，随批次一/三 TDD 红阶段落地：

1. **P1-1 证明**：A-29 新增「常量变更后 trend 与 report 历史桶逐桶相等」——旧机制（同常量现算）下该断言必假（反方推演），新机制（同谓词）下必过；
2. **P1-2 证明**：A-27 新增「retired 不阻挡：人工重加同义断言成功入行、derived 不复活 retired 行」——旧机制（全状态唯一索引）下人工重加必 constraint violation，新机制（部分唯一索引）下通过。

## V0.2 与 V0.1 差异索引（供复审导航）

| 位置 | 变更 |
|---|---|
| 文档头 | V0.1 → V0.2（待复审） |
| §0 末 | D-36 注记措辞钉死「schema 接受但零执行」 |
| §4.2 | 部分唯一索引；statementDigest 规范化函数；evidenceRefs 强制力移 confirm；R1 分母同谓词；CLI add/confirm 语义；derived 跳过口径 |
| §4.3 | 分母同谓词机制（含常量变更不分叉论证）；断言面 createdAt；coverage 按面标注 |
| §4.4 | INSIGHT_CARD_FIELD_KEYS 冻结清单 |
| §5 | report 行改同谓词表述 |
| §6 | 流程 2 重写（add/confirm 两段 + fail-fast 边界） |
| §9-4 | confirm 端证据强制 + add 侧拒假证据 |
| §10 | A-26 / A-27 / A-29 / A-30 四断言改写 |
| §11 | D-38 / D-39 两决策改写 |
| §12 / §13 / §14 | 假设 #2 机械防线注记；§12-5、§13-4、§14.1-3、§14.4-2 重写 |
| 文档尾 | 修订记录脚注 |

请反方审查官按承诺「只复审改动点，不重走全量」复审 V0.2；通过后呈综合决策官终审冻结 D-35~D-41。
