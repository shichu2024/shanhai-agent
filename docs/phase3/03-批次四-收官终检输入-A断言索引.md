# WP-3B 收官终检输入：A-1~A-11 断言索引汇编（移交 WP-3C / TASK-50）

> **用途：** 第三阶段终验收（WP-3C）收官终检输入——设计文档 V0.2 §10 验收断言清单逐条索引到实现/测试/文档位置，附消费点清单与各批验收记录索引。
> **基线：** main `a7c7494` + 批次四交付分支（本文件随批次四 PR 交付）。
> **维护纪律：** 断言位置以测试文件为准（机械可断言）；本文件只做索引汇编，不引入新断言。

## 1. A-1~A-11 断言索引

| # | 断言 | 批次 | 实现位置 | 测试位置 | 备注 |
|---|---|---|---|---|---|
| A-1 | 含密钥输出→记忆→注入→provider system 无密钥（端到端） | 二 | `src/runtime.ts:52-58`（组合根单点注入 redaction 实例）；`src/modules/memory.ts`（content 过管道） | `tests/dbRedaction.test.ts`（describe「批次二 DoD-⑥ 改进断言①」） | 改进断言①转绿 |
| A-2 | 终态任务 approve/deny 结构化拒绝（task_not_paused + 状态回显 + 无写入） | 一 | `src/modules/approval.ts`（既有结构化拒绝，纯测试补证） | `tests/approvalTerminal.test.ts`（五用例表） | 改进断言②转绿（用例即绿，申报在案） |
| A-3 | T3 CLI 级 exit 码（命中=1 / 干净=0 / 未扫描=2） | 一 | `src/scripts/releaseScan.ts`（`runReleaseScanCli` 函数化入口） | `tests/releaseScanCli.test.ts` + `tests/wp3b-batch4-releaseScan.test.ts`（批次四补：不可读 exit 2 / 位置参数告警） | 改进断言③转绿；批次四 P2-1 扩展：不可读单列计数属 exit 2 语义 |
| A-4 | 密钥样本全链 → 四 DB 落盘面行内零命中 + contextJson 结构完整性 + 实验路径继承零命中 | 二 | `src/runtime.ts`（四落盘面入库前过同一实例）；`src/modules/taskManager.ts`（input/contextJson）；`src/tools/builtin.ts`（note） | `tests/dbRedaction.test.ts`（DoD-①/③/④ 各 describe） | DB 行级断言 |
| A-5 | 同一 redaction 管道实例（Trace 与 DB 四面） | 二 | `src/runtime.ts:52-58` | `tests/dbRedaction.test.ts`（DoD-② marker 注入一致性断言） | 断言形态经反方+决策官认可（实例分裂故障形态必然红灯），不复议 |
| A-6 | inputHash 原文对账 + T1/T2 原样重跑 | 二 | `src/modules/taskManager.ts`（inputHash 原文口径在脱敏前计算） | `tests/dbRedaction.test.ts`（「跨任务同 input → inputHash 相同」）；T1/T2 存量用例（`tests/registry.test.ts` 等） | T1/T2 断言随每批全量回归 |
| A-7 | rollback 至 canary 目标 → 结构化拒绝 | 三 | `src/modules/registry.ts`（rollback 增校验，与 deprecate P2-6 对称） | `tests/batch3.test.ts`（describe「§4.5-1」） | |
| A-8 | report 分母排除 cancelled + excludedCancelled 可复算 + 分母零基线 → insufficient-sample | 三（+四分子对称） | `src/modules/report.ts`（分母 D-23 + 批次四分子 `AND t.status != 'cancelled'` 对称排除） | `tests/batch3.test.ts`（§4.5-2 两用例）+ `tests/wp3b-batch4.test.ts`（A.2 分子对称两用例） | 批次三验收 P2 由批次四收口 |
| A-9 | 三清单消费同一常量源（结构断言） | 三 | `src/modules/subclassRegistry.ts`（单一常量源） | `tests/batch3.test.ts`（§4.4-1 A-9） | 三处消费点 import 同一符号 |
| A-10 | dismiss 冷却窗内不重生、到期恢复 | 三 | `src/modules/evolution.ts`（dismissedAt + cooldownDays）；`src/config.ts`（evolution.dismissCooldownDays）；`src/db.ts`（幂等 ADD COLUMN） | `tests/batch3.test.ts`（§4.5-4 两用例） | D-25 |
| A-11 | 139→164→175→190→206 存量 + walkthrough + 行覆盖 ≥89.81% 零回退 | 全批 | — | 每批交付评论执行记录 + 反方独立复跑 | 批次四后基线：206/206（190 存量零回退 + 批次四新增 16）、行覆盖 90.94%（批次三基线 90.68% → 上升；硬底 89.81%） |

## 2. 移交物索引

| 移交物 | 位置 |
|---|---|
| 四落盘面消费点清单（批次二前置物） | `docs/phase3/02-批次二-四落盘面消费点清单.md`（§9 命名纪律登记为批次四补登） |
| 设计文档 V0.2（D-19~D-26 冻结 + 断言 A-1~A-11 原表） | `docs/phase3/01-第三阶段设计文档-初稿-方案设计师.md`（§10） |
| 规格成文面（批次一~四修订） | `docs/spec/A6-事件模型.md` §8.1/§8.2；`docs/spec/A5-版本状态机.md` §4a 等 |
| 批次一交付与验收 | TASK-49 评论 01a0bd11（交付）/ 01a0bd16（反方验收通过）；PR #9（merge `38ec6c7`） |
| 批次二交付与验收 | TASK-49 批次二交付评论（消费点清单 + DoD ①~⑦）；PR #10（merge `d6d3e5e`） |
| 批次三交付与验收 | TASK-49 评论 01a0bda9（交付，含 429 续作说明）/ 07:17 反方验收通过；PR #11（merge `a7c7494`） |
| 批次四交付（本批） | TASK-49 批次四交付评论；PR（本批） |

## 3. 终检建议项（WP-3C 输入，非新增要求）

1. 全量回归 + walkthrough + release-scan + 覆盖率（≥89.81% 硬底，批次基线 90.68% 不降）一次跑齐；
2. A-1~A-11 按本索引逐条独立复现（反方验收模式：不采信交付自述）；
3. 冻结决策 D-8~D-26 抽查无翻案（重点：D-14 幂等/聚合、D-11 管道不可削、D-21 exit 三元组、D-23 分母口径含批次四分子对称）；
4. 三底线 + T1/T2/T3 断言原样重跑（规格 V1 兼容、零数据回填核查：批次三/四新增列均为幂等 ADD COLUMN 且默认值不回填）。
