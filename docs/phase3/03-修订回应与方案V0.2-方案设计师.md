# 修订回应与方案 V0.2 · 方案设计师（第三阶段 / WP-3A）

> **文档状态：** 对反方审查报告（TASK-45 线程评论 01a0bcf1，2026-09-20）的逐项回应与修订表
> **作者角色：** 方案设计师
> **日期：** 2026-09-20
> **修订载体：** 《01-第三阶段设计文档》已整体升版 V0.2（同文件；V0.1 存于 PR #8 commit `ce7968f`）
> **处置统计：** P1 ×1 接受；P2 ×4 接受（P2-1/2/4/5）+ P2-3 部分接受；P3 ×5 接受。**无不接受项。**

---

## 1. 修订表（按角色规范格式）

| 审查问题 | 处理结果 | 处理说明 |
|---|---|---|
| **P1** 本地数据域枚举不完整（`experiment-runs/` 未入条文） | **已接受** | §4.1-2 条文枚举补 `experiment-runs/`（实验运行时自建独立 Runtime——独立 SQLite + traces，原文落盘面与主库同构，`experiment/run.ts:28-36`；gitignore/SKIP_DIRS 均覆盖、条文一并认领）；存量实验库不回填，口径与 `data/` 同款。批次二 DoD 增④「实验路径继承用例」——实验 Runtime 经同一 modules 构建，脱敏修复自动继承，实验库落盘面同样零命中（用例证明继承成立，而非假设）。承认这是 V0.1 的真实缺口：口径冻结件自身漏枚举一个已被 gitignore/SKIP_DIRS 覆盖的目录，「无条款认领」病理在冻结条文里原样存活——若非审查捕获，冻结后补认领即需走修订程序 |
| **P2-1** D-23 分母零基线未定义 | **已接受** | §4.5-2 增「排除 cancelled 后分母为 0 → `insufficient-sample`」（「不假装给答案」原则沿用）；A-8 断言补此分支；D-23 决策行同步 |
| **P2-2** 批次二缺前置消费点枚举 | **已接受** | §4.2 增「开工前置物」段：四落盘面六字段面（input / contextJson / note / failure message / expectedVsActual / rejectReason）grep 级消费点清单为开工条件，产物随批交付并入 WP-3C 验收物。承认 V0.1 只有事后回归兜底、缺前置枚举是不对称的——回归防破坏、枚举防遗漏，缺一不可 |
| **P2-3** TEXT_EXTENSIONS 枚举修补模型与 exit 2 不自洽 | **部分接受** | 接受两半：① 扩面至 11 项（+.p12/.pfx/.crt/.ini/.cfg/.conf/.env.local，每族阳性对照随批）；② 残余口径成文（A6 §8 注记：白名单外=已知未扫描残余，不属 exit 2——exit 2 只覆盖「应扫而未扫」，枚举外是显式接受面）。**择项化处理：** 未知扩展名 UTF-8 宽松解码 + 二进制探测列为批次一择项（成本低则随批做）。不完全承诺为硬 DoD 的理由：宽松解码对二进制文件的误报面需要额外阳性/阴性对照配合，将其硬塞进批次一 DoD 会把「枚举模型修补」变成「解码器工程」——择项实施 + 残余成文兜底是诚实边界。**承担的风险：** 若择项最终未做，白名单外凭据文件仍是已知残余（有注记、无扫描）。**为何可接受：** 现网扫描域为仓库根，二进制凭据文件入库本身已被 git 工作流约束；残余显式声明优于静默假装覆盖。**验证措施：** 批次一交付报告须明示择项做/未做，未做则 A6 §8 注记生效 |
| **P2-4** contextJson 脱敏实施口径未写明 | **已接受** | §4.2 处置表明确：写 snapshot 前对**解析后结构**逐消息过载荷管道，**禁止对序列化字符串做正则替换**（字符串级改写破坏 JSON 结构 → resume 即碎——正是收口批最不该引入的回归形态）；DoD 增③结构完整性断言（脱敏后可解析 + 消息数不变） |
| **P2-5** D-26 微偏离不认可 | **已接受（改判）** | **撤回微偏离，按裁定字面执行：删除退化三元 + 同步修正注释去掉 config.json 表述。** 立场改变的依据是反方引入的新事实，经本人 git 史亲验成立：`config.ts` 唯一提交 `3e1e5cf` 中两分支即同值——config.json 回退**自诞生从未生效**，不存在依赖该路径的消费者；V0.1 的「恢复已文档化回退」错在把注释声明当成了曾有行为。恢复 = 在收口批引入新行为（违反本设计原则 5），删三元 + 修注释同样使注释-行为漂移归零且零新行为。D-26 决策行与 §4.7-2 均已改判 |
| **P3-1** §5 依赖图箭头写反 | **已接受** | 修正为「批次一 → 批次二（口径冻结成文是 DB 脱敏施工的辖区前提）；批次三独立；批次四收官」 |
| **P3-2** D-25 冷却窗载体未指定 | **已接受** | 指定载体 = `config.local.json` 新段（运行时配置平台层，与 scan/exemptions 同载体、与 D-12 分权一致） |
| **P3-3** 扫描根不存在退出语义未定义 | **已接受** | 归入 exit 2（应扫而未扫），§4.3-2 与 D-21 成文，批次一增对应用例 |
| **P3-4** SKIP_DIRS「双防线」措辞易误读 | **已接受** | §4.1-2 措辞精确化：发布防线 = `.gitignore` 排除**一道**；SKIP_DIRS 是扫描范围定义，机制上不承担防泄漏职能、不计为防线——「不存在双防线，防线单一正是批次二机制化的理由」（把误读风险反转为机制化论据） |
| **P3-5** policiesOf 重复手写实现未认领 | **已接受** | §4.5 增第 6 项：policiesOf 收敛为单一实现（registry 层导出），CLI 与测试两处消费同一函数，随批次三交付（终验收 P3-2 认领） |

### 交审点三答的回应

| 交审点 | 反方立场 | 设计师回应 |
|---|---|---|
| D-21 exit 2 三元组 | 支持维持 + 两补强（allow-exempted-root 须有真实用例；根不存在归 exit 2） | **两补强全部落稿**：§4.3-2 / 批次一 DoD / D-21 均已更新 |
| D-24 policiesOf 维持回溯 | 同意，但成文必须完整（已弃用版本声明仍统治的边角） | **边角语义落稿**：§4.5-5 + D-24 明示「遍历全量倒序、不按指针/状态，已弃用版本仍统治」，随终审冻结 |
| D-26 config.ts:33 | 不认可微偏离，按裁定字面 | **接受改判**（见上表 P2-5），微偏离撤回 |

## 2. 无不接受项的说明

本修订无不接受条目。一点说明：P2-3 标「部分接受」——接受扩面与残余成文（两者均已落稿为硬 DoD/注记），未接受的是把「未知扩展名宽松解码探测」升为硬 DoD（择项化处理）；风险承担与验证措施见修订表内。其余十二条全部全量接受并落稿。

## 3. V0.1 → V0.2 变更清单（供复审导航）

| 位置 | 变更 |
|---|---|
| 文档头部 | 升版 V0.2 + 修订记录 |
| §4.1-2 | `experiment-runs/` 枚举补全；防线措辞精确化（P1 / P3-4） |
| §4.2 | contextJson 结构级实施口径（P2-4）；开工前置物消费点清单（P2-2）；DoD 重排为⑦项（增结构完整性、实验路径继承；P1） |
| §4.3-2 | 扫描根不存在 → exit 2；allow-exempted-root 须有真实用例（P3-3 / D-21 补强） |
| §4.3-4 | 扩展名扩面至 11 项 + 残余口径成文 + 宽松解码择项（P2-3） |
| §4.4/§4.5 | 无变更（§4.5-2 零基线 / §4.5-5 边角成文 / 新增 §4.5-6 收敛） |
| §4.5-2 | 分母零基线 → insufficient-sample（P2-1） |
| §4.5-5/6 | policiesOf 边角成文 + 实现收敛（D-24 补强 / P3-5） |
| §4.7-2 | config.ts:33 改判按裁定字面（P2-5） |
| §5 | 三批次 DoD 扩充；依赖图箭头修正（P3-1） |
| §6 | D-19/20/21/23/24/25/26 七行更新（D-26 改判） |
| §9.4-3 | D-26 标记结清 |
| §10 | A-4 / A-8 扩充 |
| 尾注 | 修订记录与复审请求 |

---

*V0.2 待反方审查官复审（重点复核 P1 与 P2-5 改判是否落净、P2-3 择项化处理是否可接受）。复审通过后呈综合决策官终审冻结 D-19~D-26。*
