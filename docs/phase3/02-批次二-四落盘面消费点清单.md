# 批次二开工前置物：四落盘面六字段全消费点清单

> **用途：** 设计文档 V0.2 §4.2「开工前置物（V0.2 增，P2-2 采纳）」——回归防破坏、枚举防遗漏。
> **枚举方法：** grep 级（`contextJson` / `row.input` / `notes.md` / `rejectReason` / `expectedVsActual` / `inputHash` 全量检索 src/，基线 main `38ec6c7`）。
> **交付批次：** WP-3B 批次二；并入 WP-3C 验收物。

## 1. `task_record.input`

| # | 消费点 | 类型 | 脱敏后语义 | 处置 |
|---|---|---|---|---|
| 1-1 | `src/modules/taskManager.ts:197-200`（INSERT 落库） | **写入面** | 本批处置：INSERT 前过管道（payload 层） | 改造 |
| 1-2 | `src/modules/taskManager.ts:370`（`JSON.parse(row.input)` → `runAgentLoop` 的 `ctx.input`） | 读·执行 | 续跑/执行模型见 redacted 版 input（与 §7-#2 contextJson 同口径：脱敏单向，行为漂移如实记录不判失败） | 语义注记（§3-R1） |
| 1-3 | `src/runtime/executor.ts:147`（`messages[0] = JSON.stringify(ctx.input)` → provider 请求） | 读·执行 | 同 1-2（模型用户消息为 redacted 文本） | 语义注记（§3-R1） |
| 1-4 | Trace `task_created` 事件载荷 `input` 键（`taskManager.ts:203`，与 DB 行双写） | 读·留痕 | Trace 管道已脱敏（既有行为）；本批后 DB 行与 Trace 改写结果一致（§9.1-3 双写一致性） | 一致性断言 |
| 1-5 | **重算 input 摘要的潜在消费者：无 DB 侧行级重算**。`inputHash` 全部计算点（`taskManager.ts:174,188,202`）均取**内存中的原始 input**（INSERT 前计算），不从 `row.input` 重算；`registry.ts:109,123,394` 的 `inputHash` 是 specSnapshot/agentId 摘要，与任务 input 无关 | 摘要 | 原文口径经 Trace `task_created.inputHash` 与 audit `inputHash` 列保留，T1 跨任务对账不破 | 回归断言（DoD-⑤） |
| 1-6 | `src/modules/report.ts`（63/105/112/167/178/182 行：task_record 仅消费计数/状态/assignmentSource/时间戳） | 读·统计 | 不消费 input 值——零影响 | 无处置 |
| 1-7 | `src/cli.ts:149`（task create 从文件读原始 input 传入 createTask——内存侧，非 DB 读） | 入口 | 入口原文照常进入 contract 校验（校验先于脱敏，顺序不变） | 无处置 |

## 2. `pause_snapshot.contextJson`

| # | 消费点 | 类型 | 脱敏后语义 | 处置 |
|---|---|---|---|---|
| 2-1 | `src/modules/taskManager.ts:427-435`（persistPause 序列化 `contextOnly` → saveSnapshot） | **写入面** | 本批处置：解析后结构（PauseContext 对象）逐字段过管道后序列化——禁止对序列化字符串做正则替换（D-20 冻结口径） | 改造 |
| 2-2 | `src/modules/taskManager.ts:291-303`（resume：`JSON.parse(contextJson)` → `resumeState` → executeLoop） | 读·执行 | 续跑模型见 redacted 版上下文（§7-#2 已知取舍，设计冻结） | 语义注记（§3-R2）+ 结构完整性断言（DoD-③） |
| 2-3 | `src/modules/approval.ts:118-129`（`show()`：从 contextJson 取 pending 调用 args 计算 `argsDigest` 展示） | 读·展示 | digest 改对 redacted 版 args 计算（快照内实际内容的指纹——展示用，无对账消费者；原文 args 经 Trace `tool_called` 事件的 argsDigest 留痕） | 语义注记（§3-R3） |
| 2-4 | `src/runtime/executor.ts:34-`（PauseContext 结构注释）与 `resultsSoFar`/`assistantToolCalls.args`（快照内嵌挂起批次的工具参数原文） | 结构 | 管道递归 walk 覆盖（messages / assistantToolCalls / resultsSoFar 全部自由文本） | 改造（随 2-1） |

## 3. note（`data/notes.md`）

| # | 消费点 | 类型 | 脱敏后语义 | 处置 |
|---|---|---|---|---|
| 3-1 | `src/tools/builtin.ts:102-107`（note-append 追加 `- <ts> <note>\n`） | **写入面** | 本批处置：`args.note` 过管道后再追加（工具副作用输出过管道，§4.2 表第 3 行） | 改造 |
| 3-2 | 运行时无任何 reader（grep `notes.md` 仅写入点 + T3 扫描面）；`data/` 属本地数据域（A6 §8 v1.3） | 读 | 零消费者——无语义影响 | 无处置 |

## 4. `failure_record.message` / `failure_record.expectedVsActual`

| # | 消费点 | 类型 | 脱敏后语义 | 处置 |
|---|---|---|---|---|
| 4-1 | `src/modules/recorders.ts:32-57`（INSERT） | **写入面** | 本批处置：message（字符串）+ expectedVsActual（结构化 walk，`*Digest`/`*Hash` 键零改写）入库前过管道 | 改造 |
| 4-2 | `src/modules/recorders.ts:59-61`（`forTask` SELECT——调用方见 4-3） | 读 | 返回 redacted 行 | 随 4-1 |
| 4-3 | `src/modules/report.ts:111-112`（仅 `COUNT(DISTINCT f.taskId)`——计数，不消费值） | 读·统计 | 零影响 | 无处置 |
| 4-4 | `src/modules/stateManager.ts:80`（崩溃恢复标记写 expectedVsActual `{expected:'终态或无记录', actual: t.status}`） | 写·途经 | 值为状态字符串，无用户原文；途经 record() 自动过管道 | 随 4-1 |
| 4-5 | CLI/测试直查（人工审阅面） | 读·展示 | 展示 redacted 文本（预期行为） | 无处置 |

## 5. `audit_events.rejectReason`

| # | 消费点 | 类型 | 脱敏后语义 | 处置 |
|---|---|---|---|---|
| 5-1 | `src/modules/recorders.ts:104-127`（rejectedRequest INSERT） | **写入面** | 本批处置：JSON 结构级 walk 后再 stringify（rejectReason 恒为 `JSON.stringify(issues数组)`——按结构过管道，不做字符串级替换） | 改造 |
| 5-2 | `src/evidence.ts:38-58`（queryT2 SELECT rejectReason——T2 审阅面展示） | 读·展示 | T2 答案展示 redacted 文本（人工审阅语义不变：看得见拒绝原因结构，看不见被拒原文中的密钥） | 语义注记（§3-R4） |
| 5-3 | 写入方：`taskManager.ts:175,189`（issues 含 input 实际值片段）/ `registry.ts:110,124,394`（spec 校验 issues） | 写·途经 | 途经 record() 自动过管道 | 随 5-1 |
| 5-4 | `audit_events.payload`（versionEvent 列）**不在六字段面内**（D-20 辖区），本批不动——载荷为 spec/工具登记摘要，无任务 input 原文通道 | — | 范围外显式登记 | 无处置（范围控制） |

## 6. 实验路径（`experiment-runs/`）

| # | 消费点 | 说明 |
|---|---|---|
| 6-1 | `src/experiment/run.ts:31-36`（`new Runtime({dataDir: experiment-runs/hyp4-*, ...})`，不传 redaction → 默认规则集） | 同一 Runtime 构造点消费同一改造——四落盘面脱敏自动继承（DoD-④ 继承用例验证） |

## 7. 枚举完备性声明

- 检索式：`\.input\b`、`row.input`、`contextJson`、`notes.md`、`rejectReason`、`expectedVsActual`、`inputHash`、`forTask(`、`FROM task_record`、`FROM failure_record`、`FROM audit_events`（src/ 全量，排除仅注释命中）。
- 唯一四个写入面 + 上述读点全覆盖；**未发现第六字段面之外的 input 原文重算者**（§4.2「重算 input 摘要的潜在消费者」核查结论：inputHash 计算点全部位于内存原文侧，见 1-5）。

## 8. 语义影响汇总（随批成文，进 A6 §8 注记视野）

| # | 影响点 | 口径 |
|---|---|---|
| R1 | 执行/续跑模型见 redacted 版 input（1-2/1-3） | 与记忆 content、contextJson 同口径：脱敏单向，行为漂移如实记录不判失败（§7-#2 外推） |
| R2 | resume 见 redacted 上下文（2-2） | §7-#2 设计冻结 |
| R3 | approval show 的 argsDigest 改对 redacted args 计算（2-3） | 展示指纹随快照实际内容；原文指纹经 Trace argsDigest 留痕 |
| R4 | T2/evidence 审阅面展示 redacted rejectReason（5-2） | 审阅语义保留（结构可见），密钥不可见——T3/T2 断言不受影响（T2 消费的是事件存在性与计数） |

## 9. 命名纪律登记（批次三验收 #2 裁定 + 批次三 P3-1 裁定，2026-09-20 批次四补登记）

| # | 纪律 | 裁定来源 | 说明 |
|---|---|---|---|
| 9-1 | **canaryRounds 切轮事件命名纪律：** report 跨轮警示列按 `canary_configured` / `version_promoted` / `version_rollback` 三类审计事件切轮——当前事件模型下该集合完备；**未来新增带轮次语义的审计事件时，须同步扩展 `report.ts` 切轮识别**，否则跨轮窗口漏警示 | 批次三验收 #2（决策官保留裁定） | 消费点：`src/modules/report.ts` boundaryEvents 查询 |
| 9-2 | **`*Hash` 后缀命名纪律：** 该后缀仅用于摘要字段（脱敏排除表 `(Digest|Hash)$` 依赖键名信任）——**新增承载原文的键严禁以 Hash/Digest 结尾**；新增 `*Hash`/`*Digest` 键时在本清单登记 | 批次二验收 P3-1 + 批次三 P3-1 裁定（冻结不翻案） | 消费点：`src/modules/redaction.ts` EXCLUDED_KEY_SUFFIX |
